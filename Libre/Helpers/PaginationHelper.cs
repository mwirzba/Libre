using Microsoft.EntityFrameworkCore;
/*
namespace Libre.Helpers
{
    public class PaginationHelper<T>: DataHelper<T> where T : class
    {
        public PaginationHelper(int pageNumber, int pageSize, DbSet<T> data): base(data)
        {
            this.pageNumber = pageNumber;
            this.pageSize = pageSize;
        }

        protected int pageSize;
        protected int pageNumber;
        public void ToPagedList()
        {
            return data.ToPagedList(pageNumber, pageSize);
        }
    }
}
*/